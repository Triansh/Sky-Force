// import * as THREE from 'three';
// import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader';

// function loadGLTF(scene, path) {
//     let planeLoader = new GLTFLoader();
//     let model;
//     planeLoader.setPath('/assets/models/').load('toyPlane.glb', gltf => {
//         model = gltf.scene;
//         model.scale.set(0.2, 0.2, 0.2);
//         scene.add(model);
//         model.position.x = 0;
//         model.position.y = 8;
//         model.position.z = 0;
//         // makeLines(scene, model.position);
//         model.scale.multiplyScalar(10);
//         model.rotation.y = (Math.PI / 2) * 0.25;
//         // obstacleController.add(scene, model.position.clone());
//         // console.log(model);
//     });

//     return model;
// }

// export default loadGLTF;
